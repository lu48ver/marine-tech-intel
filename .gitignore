# Build output (generated by scripts/build_site.py)
build/

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS
.DS_Store
Thumbs.db

# Local archive of spec materials (extracted into files/)
files.zip

# Claude Code local settings (keep launch.json, ignore machine-local prefs)
.claude/settings.local.json

# Scratch / temp files used during development
_commitmsg.txt
_probe*

# Local API key for testing (never commit secrets)
.openai_key
.env
